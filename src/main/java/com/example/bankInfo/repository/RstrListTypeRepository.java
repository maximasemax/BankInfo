package com.example.bankInfo.repository;

import com.example.bankInfo.entity.RstrListTypeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RstrListTypeRepository extends JpaRepository<RstrListTypeEntity, Long> {
}
